# Targets
FILE(GLOB DATRIE_SRC ${CMAKE_CURRENT_SOURCE_DIR}/datrie/*.c)
FILE(GLOB KMLIB_SRC ${CMAKE_CURRENT_SOURCE_DIR}/kmlib/src/*.c)
ADD_EXECUTABLE(trieBCD trieBCD_main.c)
ADD_LIBRARY(tbd STATIC ${DATRIE_SRC} ${KMLIB_SRC} trieBCD.c)
TARGET_LINK_LIBRARIES(trieBCD ${ZLIB_LIBRARIES} tbd)
INSTALL(TARGETS trieBCD DESTINATION "bin")
