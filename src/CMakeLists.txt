# Targets
FILE(GLOB DATRIE_SRC ${CMAKE_CURRENT_SOURCE_DIR}/datrie/*.c)
FILE(GLOB KMLIB_SRC ${CMAKE_CURRENT_SOURCE_DIR}/kmlib/src/*.c)
ADD_LIBRARY(axelib STATIC ${DATRIE_SRC} ${KMLIB_SRC} axe.c)
ADD_EXECUTABLE(axe main.c)
TARGET_LINK_LIBRARIES(axe ${ZLIB_LIBRARIES} axelib)
INSTALL(TARGETS axe DESTINATION "bin")
